{
  "name": "dottorbot-ui",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i src/input.css -o dist/style.css",
    "build:js": "esbuild src/main.js --bundle --outfile=dist/chat.js",
    "build": "npm run build:css && npm run build:js",
    "test": "echo 'no tests'"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.3",
    "esbuild": "^0.19.12"
  }
}
